NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never prefix with NEXT_PUBLIC_ and never import this in client-side
# or route-handler code that runs on behalf of a logged-in user's session.
SUPABASE_SERVICE_ROLE_KEY=
